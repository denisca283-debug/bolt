// Explicit projection: private fields must never enter public browser state.
// Keep aligned with the profile column grants in the security migration.
export const PROFILE_FIELDS = 'id,full_name,public_slug,city,country,gender,avatar_url,about,availability_status,created_at,updated_at';
